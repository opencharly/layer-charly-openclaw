# 2026.246.0100 — chore: note OpenClaw 2.0 in the openclaw skill prose

## Summary

The concept candy's `openclaw-layer` skill now names the OpenClaw 2.0 version
(npm `openclaw@2026.8.x`) in its description. The candy's own CalVer moves because
changed content must ship under a new version rather than reusing one already
released. No other content changes — the desktop/full/full-ml skills already describe
the composition, which is unchanged by the 2.0 gateway bump.

## How tested

```
$ charly box validate
exit 0
```

Run with the charly version this repo's own `deploy.yml` clones and builds. The diff
is confined to `charly.yml`: the version line and one skill description. This is a
documentation-only candy (no install content), so there is no runtime path to
exercise.

## Rulebook compliance

- **R0 (skills)** - `/charly-image:layer` for the candy/skill model;
  `/charly-internals:git-workflow` for PR-only landing and tag-on-merge.
- **R1 (RCA)** - the prose update tracks the 2.0 bump in pod-openclaw; the skill must
  not advertise a stale version.
- **R2 (no deferral)** - the prose ships in the same batch as the 2.0 bump.
- **R3 (no duplication)** - the version is named once, in the skill description.
- **R4 (no workarounds)** - N/A.
- **R5 (hard cutover)** - the stale version reference is gone.
- **R6 (git safety)** - feature branch, no force-push, no hook bypass.
- **R7 / R10 (prove the gate)** - `charly box validate` on the committed tree with the
  repo's own pinned charly.
- **R8 (generation)** - N/A: no generated artifact, no CUE input in this repo.
- **R9 (artifact integrity)** - N/A: no image is built here.
- **ADE (candy description/plan)** - `description:` and `plan:` untouched (the
  change is in a skill entity's description).
- **SDD (CUE source)** - N/A: no schema authored or generated here.

## Change classification

- **Change class:** skill prose update + the candy's own CalVer bump. Docs only.
- **Verification gate:** `charly box validate` under the repo's pinned charly, exit 0
- **Attribution tier:** `documentation reviewed` - docs-only change class; the gate
  was executed on a real checkout with the exit code recorded

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*
